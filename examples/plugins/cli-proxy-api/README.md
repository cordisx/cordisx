# CLIProxy Providers

This example re-exports the built-in plugin. Its user-facing behavior, configuration, identity, permissions, unavailable states, and experimental boundary are documented in the [canonical product README](../../../packages/cli/src/plugins/cli-proxy-api/README.md).
