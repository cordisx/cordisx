# Plugin Console Showcase

Development-only fixture for the plugin detail **运行状态** DevTools Console. It
uses native variadic `console.*` calls and exercises one successful synchronous
Host capability plus one permissioned Platform call.

The scoped console is intentionally only a supplement. Host API entries are
created by the capability aspect, not by this plugin.
