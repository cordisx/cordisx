/** The packaged index.html's static splash mark, never an application logo. */
export const NATIVE_STARTUP_MARK_SELECTOR = 'body > #root > .startup-loader[aria-hidden="true"] > .startup-loader__logo'
